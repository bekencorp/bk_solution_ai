/* SDK bk_init includes vnd_cal.h from the project CP directory.
 * Keep the header lookup working while the implementation lives on the
 * beken_robot base. */
#include "../../beken_robot/cp/vnd_cal.h"
